# 加一条指令要做什么

忘了就看这个。顺序别跳。

---

## 第 0 步：先别碰代码

打开 MIPS 手册（或题面），把这条指令的 RTL 抄下来。例如：

```
movn rd, rs, rt:   if GPR[rt] != 0 then GPR[rd] <- GPR[rs]
```

**你要加的不是"一条指令"，是这段 RTL 描述的数据流动。**

---

## 第 1 步：回答 5 个问题

只看 RTL，不看代码。答案写在纸上。

| # | 问题 | movn 的答案 |
|---|------|------------|
| 1 | 读哪两个寄存器？ | rs、rt（默认就是这俩，几乎不用动） |
| 2 | ALU 算什么？两个输入是谁？ | 不用 ALU |
| 3 | **写寄存器吗？写哪个？写什么值？** | 写（有条件）；写 rd；写 rs 的值 |
| 4 | 写内存吗？ | 不写 |
| 5 | 下一条 PC 是什么？ | PC+4 |

> CPU 里只有三样东西有状态：**GRF、DM、PC**。
> 一条指令能做的全部事情，就是改这三样中的若干样。
> 问题 3/4/5 分别对应这三样。问题 1/2 只是"为了改它们，得先算出什么"。

---

## 第 2 步：把 5 个答案翻译成信号

| 你的答案 | 对应信号 | 改哪 |
|---------|---------|------|
| 一定写寄存器 | `RegWrite` | ctrl.v 或上它 |
| 看数据才写寄存器 | `CondWrite` + `CMPop` | ctrl.v 两处 |
| 写哪个寄存器 | `A3sel` | ctrl.v |
| 写什么值 | `WDsel` | ctrl.v |
| ALU 的 b 口接立即数 | `ALUSrc` | ctrl.v |
| 立即数要符号扩展 | `EXTop` | ctrl.v |
| ALU 算什么 | `ALUop` | ctrl.v |
| 写内存 | `MemWrite` | ctrl.v |
| PC 从哪来 | `NPCop` | ctrl.v |
| 判什么条件 | `CMPop` | ctrl.v |

**注意这一列全是 `ctrl.v`。** 架构没问题的话，加指令就只改 ctrl.v 一个文件。

只有当**现有的档位不够用**时，才需要动别的文件 —— 见第 3 步。

---

## 第 3 步（可能不需要）：新增一个档位 = 三处法则

发现"WDsel 里没有我要的那一路"时：

```
1. def.vh      `define WD_XXX  4'd?      ← 定义编号
2. 收货的地方   mips.v 的 wd 那个三目链加一路 ← 真正接线
3. ctrl.v      WDsel = ... xxx ? `WD_XXX ← 选中它
```

**三处缺任何一处都是静默失败**，编译器一个字都不说：

| 缺哪一处 | 症状 |
|---------|------|
| 缺 1 | 宏未定义，这个会报错（唯一会报的） |
| 缺 2 | 走 default，值悄悄错 |
| 缺 3 | 新代码永远不执行，像没加过一样 |

各档位的"收货方"：

| 档位 | 收货方 |
|------|-------|
| `WD_*` | mips.v 的 `assign wd = ...` |
| `A3_*` | mips.v 的 `wire [4:0] a3 = ...` |
| `ALU_*` | alu.v 的 case |
| `NPC_*` | npc.v 的 case |
| `CMP_*` | cmp.v 的 case |

---

## 第 4 步：最省事的路径

ctrl.v 里有四个分类：

```verilog
wire calc_r = addu | subu;              // 读 rs/rt，写 rd
wire calc_i = addiu | xori | lui | ori; // 读 rs，写 rt，第二操作数是立即数
wire branch = beq | bne;
wire link   = jal | jalr;               // 把 PC+4 写进寄存器
```

**新指令能塞进某一类，就只加两行**：一行译码 wire，一行或进分类。

例：加 `and`（R 型，rd = rs & rt）

```verilog
wire and_ = R & (funct == 6'h24);   // 1. 译码（and 是关键字，加下划线）
wire calc_r = addu | subu | and_;   // 2. 塞进 calc_r —— RegWrite/A3sel/WDsel 全自动对了
assign ALUop = ... : and_ ? `ALU_AND : ...   // 3. 只剩 ALU 要算什么
```
再按三处法则补 `ALU_AND`：def.vh 加一行、alu.v 加一个 case。**总共 5 行。**

---

## 第 5 步：自查（都是静默失败，编译器不报）

- [ ] 三处法则的第 2、3 处都做了？
- [ ] 位宽写的是 `4'd?` 不是 `3'd?`？（控制线统一 4 位）
- [ ] ctrl.v 里新加的 `output` 都有对应的 `assign`？
      **无驱动的 output = `z`，`z & x = x`，`if (x)` 走假分支，一声不吭**
- [ ] 新加的 wire 声明在**使用它的那行之前**？
- [ ] 位切片宽度对得上？（`instr[10:5]` 是 6 位，塞进 5 位线会静默丢掉一位）
- [ ] 判 `>= 0` 用了 `$signed()`？（无符号数 `>= 0` 恒为真）
- [ ] mips.v 的 ctrl 实例化里，新端口连上了？

---

## 第 6 步：对拍

```bash
./run.sh 你的.asm
```

写测试程序时：
- **不要写助记符 `nop`**，要空转写 `sll $0, $0, 0`（MARS 只给后者记写事件）
- 条件类指令要**两个方向都测**：条件成立跳一次，不成立走一次
- 边界要测：`$0` 作目的寄存器、负数、0

---

## 一句话版本

> **加指令 = 在 ctrl.v 里说清楚"这条指令怎么改 GRF / DM / PC"。
> 档位不够就按三处法则加一档。其他文件都不用动。**
