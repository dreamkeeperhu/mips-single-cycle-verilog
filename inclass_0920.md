# 课上模拟 · 2026-09-20 · P4 加指令三道

限时 120 分钟。三道过两道算通过。
下列指令均为课程组自编，不属于 MIPS 指令集。

---

## T1 `swgt`

```
swgt  rt, offset(rs)
```

```
 31    26 25   21 20   16 15                    0
┌────────┬───────┬───────┬───────────────────────┐
│ 101111 │  rs   │  rt   │        offset         │
└────────┴───────┴───────┴───────────────────────┘
```

```
if GPR[rt] > GPR[rs] then
    Mem[GPR[rs] + sign_ext(offset)] ← GPR[rt]
```

比较为有符号比较。

---

## T2 `lwadd`

```
lwadd  rt, offset(rs)
```

```
 31    26 25   21 20   16 15                    0
┌────────┬───────┬───────┬───────────────────────┐
│ 100111 │  rs   │  rt   │        offset         │
└────────┴───────┴───────┴───────────────────────┘
```

```
GPR[rt] ← Mem[GPR[rs] + sign_ext(offset)] + GPR[rt]
```

---

## T3 `bclz`

```
bclz  rs, rt, offset
```

```
 31    26 25   21 20   16 15                    0
┌────────┬───────┬───────┬───────────────────────┐
│ 011101 │  rs   │  rt   │        offset         │
└────────┴───────┴───────┴───────────────────────┘
```

```
a ← CLZ(GPR[rs])
b ← CLZ(GPR[rt])
if a = b then
    PC ← PC + 4 + sign_ext(offset || 0²)
else
    GPR[rt] ← max(a, b)
    PC ← PC + 4
```

`CLZ(x)` 为 x 的二进制表示中最高位起连续 0 的个数；`CLZ(0x00000000) = 32`。
