# 课上模拟 · 2026-09-20 · 第二套

限时 120 分钟。三道过两道算通过。
下列指令均为课程组自编，不属于 MIPS 指令集。

---

## T1 `rorv`

```
rorv  rd, rs, rt
```

```
 31    26 25   21 20   16 15   11 10    6 5     0
┌────────┬───────┬───────┬───────┬───────┬────────┐
│ 000000 │  rs   │  rt   │  rd   │ 00000 │ 111101 │
└────────┴───────┴───────┴───────┴───────┴────────┘
```

```
n ← GPR[rs][4:0]
GPR[rd] ← ROR(GPR[rt], n)
```

`ROR(x, n)` 为 x 循环右移 n 位：从低位移出的 n 个比特，按原顺序依次填回最高位。
`n = 0` 时 `ROR(x, 0) = x`。

---

## T2 `apc`

```
apc  rt, offset
```

```
 31    26 25   21 20   16 15                    0
┌────────┬───────┬───────┬───────────────────────┐
│ 011110 │ 00000 │  rt   │        offset         │
└────────┴───────┴───────┴───────────────────────┘
```

```
GPR[rt] ← PC + 4 + sign_ext(offset || 0²)
```

rs 字段恒为 `00000`，不参与运算。

---

## T3 `dbnz`

```
dbnz  rs, offset
```

```
 31    26 25   21 20   16 15                    0
┌────────┬───────┬───────┬───────────────────────┐
│ 010110 │  rs   │ 00000 │        offset         │
└────────┴───────┴───────┴───────────────────────┘
```

```
GPR[rs] ← GPR[rs] − 1
if GPR[rs] − 1 ≠ 0 then
    PC ← PC + 4 + sign_ext(offset || 0²)
else
    PC ← PC + 4
```

写回值与跳转条件用的是同一个"减 1 之后"的结果。
rt 字段恒为 `00000`，不参与运算。
